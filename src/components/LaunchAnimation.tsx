'use client'

// Launch splash temporarily disabled — was interrupting the
// /onboarding/subscribe handoff on mobile. The previous mobile-only
// seaplane-clip implementation lives in git history at commit 2b02159
// if/when we want it back; restore that file to re-enable.
export default function LaunchAnimation() {
  return null
}
